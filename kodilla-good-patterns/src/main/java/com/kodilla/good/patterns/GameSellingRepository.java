package com.kodilla.good.patterns;

import java.time.LocalDateTime;

public class GameSellingRepository implements SellingRepository{
    @Override
    public void createRepository(User user, Product product, LocalDateTime requestDate) {

    }
}
